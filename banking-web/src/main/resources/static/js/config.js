// Configuration for Banking Web Application
// This file can be dynamically generated or modified for different environments

window.BANKING_CONFIG = {
    // Default API URL (can be overridden by environment)
    API_BASE_URL: window.BANKING_API_URL || 'http://localhost:8080/api/v1/banking'
};
